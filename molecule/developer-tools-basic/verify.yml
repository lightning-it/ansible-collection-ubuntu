---
- name: Verify developer tools syntax coverage
  hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name: Assert lit.ubuntu.developer_tools coverage stub ran
      ansible.builtin.assert:
        that:
          - developer_tools_molecule_coverage_mode | default('') == 'syntax_stub'
        quiet: true
