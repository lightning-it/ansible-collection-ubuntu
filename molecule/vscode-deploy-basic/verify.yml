---
- name: Verify vscode deploy syntax coverage
  hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name: Assert lit.ubuntu.vscode_deploy coverage stub ran
      ansible.builtin.assert:
        that:
          - vscode_deploy_molecule_coverage_mode | default('') == 'syntax_stub'
        quiet: true
