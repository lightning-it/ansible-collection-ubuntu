---
- name: Verify vscode config syntax coverage
  hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name: Assert lit.ubuntu.vscode_config coverage stub ran
      ansible.builtin.assert:
        that:
          - vscode_config_molecule_coverage_mode | default('') == 'syntax_stub'
        quiet: true
