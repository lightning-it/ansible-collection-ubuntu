---
- name: Verify firefox config syntax coverage
  hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name: Assert lit.ubuntu.firefox_config coverage stub ran
      ansible.builtin.assert:
        that:
          - firefox_config_molecule_coverage_mode | default('') == 'syntax_stub'
        quiet: true
