---
- name: Verify vscode destroy syntax coverage
  hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name: Assert lit.ubuntu.vscode_destroy coverage stub ran
      ansible.builtin.assert:
        that:
          - vscode_destroy_molecule_coverage_mode | default('') == 'syntax_stub'
        quiet: true
