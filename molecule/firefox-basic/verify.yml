---
- name: Verify firefox syntax coverage
  hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name: Assert lit.ubuntu.firefox coverage stub ran
      ansible.builtin.assert:
        that:
          - firefox_molecule_coverage_mode | default('') == 'syntax_stub'
        quiet: true
