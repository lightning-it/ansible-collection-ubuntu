---
- name: Verify firefox destroy syntax coverage
  hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name: Assert lit.ubuntu.firefox_destroy coverage stub ran
      ansible.builtin.assert:
        that:
          - firefox_destroy_molecule_coverage_mode | default('') == 'syntax_stub'
        quiet: true
