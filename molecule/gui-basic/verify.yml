---
- name: Verify gui syntax coverage
  hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name: Assert lit.ubuntu.gui coverage stub ran
      ansible.builtin.assert:
        that:
          - gui_molecule_coverage_mode | default('') == 'syntax_stub'
        quiet: true
