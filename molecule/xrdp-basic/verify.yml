---
- name: Verify xrdp syntax coverage
  hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name: Assert lit.ubuntu.xrdp coverage stub ran
      ansible.builtin.assert:
        that:
          - xrdp_molecule_coverage_mode | default('') == 'syntax_stub'
        quiet: true
