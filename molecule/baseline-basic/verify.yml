---
- name: Verify baseline syntax coverage
  hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name: Assert lit.ubuntu.baseline coverage stub ran
      ansible.builtin.assert:
        that:
          - baseline_molecule_coverage_mode | default('') == 'syntax_stub'
        quiet: true
