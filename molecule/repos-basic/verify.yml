---
- name: Verify repos syntax coverage
  hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name: Assert lit.ubuntu.repos coverage stub ran
      ansible.builtin.assert:
        that:
          - repos_molecule_coverage_mode | default('') == 'syntax_stub'
        quiet: true
