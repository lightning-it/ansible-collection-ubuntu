---
- name: Verify users syntax coverage
  hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name: Assert lit.ubuntu.users coverage stub ran
      ansible.builtin.assert:
        that:
          - users_molecule_coverage_mode | default('') == 'syntax_stub'
        quiet: true
