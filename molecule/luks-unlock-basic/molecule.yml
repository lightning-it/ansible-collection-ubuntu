---
dependency:
  name: galaxy
driver:
  name: docker
platforms:
  - name: luks_unlock
    image: ubuntu:24.04
    pre_build_image: true
    command: sleep infinity
    network_mode: host
provisioner:
  name: ansible
  playbooks:
    converge: converge.yml
    verify: verify.yml
  inventory:
    hosts:
      all:
        hosts:
          luks_unlock:
            ansible_connection: docker
verifier:
  name: ansible
