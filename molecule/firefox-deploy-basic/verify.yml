---
- name: Verify firefox deploy syntax coverage
  hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name: Assert lit.ubuntu.firefox_deploy coverage stub ran
      ansible.builtin.assert:
        that:
          - firefox_deploy_molecule_coverage_mode | default('') == 'syntax_stub'
        quiet: true
