---
argument_specs:
  main:
    short_description: Configure Firefox on Ubuntu using the shared lit.ubuntu.firefox role.
    options:
      firefox_config_enabled:
        type: bool
        default: true
