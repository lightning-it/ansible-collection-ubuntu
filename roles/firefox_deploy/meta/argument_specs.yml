---
argument_specs:
  main:
    short_description: Install Firefox on Ubuntu using the shared lit.ubuntu.firefox role.
    options:
      firefox_deploy_enabled:
        type: bool
        default: true
