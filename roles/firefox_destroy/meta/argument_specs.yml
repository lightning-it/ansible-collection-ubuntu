---
argument_specs:
  main:
    short_description: Remove Firefox-managed state on Ubuntu using the shared lit.ubuntu.firefox role.
    options:
      firefox_destroy_enabled:
        type: bool
        default: true
