# Managed by lit.ubuntu.forward_proxy. Local changes are overwritten.
http_proxy={{ forward_proxy_url }}
https_proxy={{ forward_proxy_url }}
HTTP_PROXY={{ forward_proxy_url }}
HTTPS_PROXY={{ forward_proxy_url }}
no_proxy={{ forward_proxy_no_proxy | join(',') }}
NO_PROXY={{ forward_proxy_no_proxy | join(',') }}
