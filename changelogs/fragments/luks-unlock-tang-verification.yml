---
bugfixes:
  - >-
    luks_unlock - Enforce the declared Tang signing thumbprint during preflight and
    cached-advertisement enrollment, verify persisted bindings using Clevis's
    URL-only representation, and bound an exact-keyslot online unlock test without
    exposing key material.
minor_changes:
  - >-
    luks_unlock - Add an explicitly non-idempotent, one-run recovery option to
    rebuild initramfs after an existing Clevis binding passes every verifier.
