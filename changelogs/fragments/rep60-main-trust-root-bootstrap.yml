---
bugfixes:
  - >-
    Restore the protected REP-60 exact-revision review trust root on main,
    bind workflow dispatches to the selected base commit, and fail closed on
    non-object GitHub pull-request JSON.
