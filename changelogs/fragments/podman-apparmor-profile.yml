---
bugfixes:
  - >-
    Add an opt-in Podman AppArmor profile repair and targeted reload so Ubuntu
    hosts can restore the package-compatible unconfined attachment profile
    before validating the direct Podman executable; also reconcile the system
    Podman socket to its explicitly configured state.
  - >-
    Allow opt-in Node.js runtime options for npm and npx operations so
    hosts without working IPv6 egress can explicitly prefer IPv4 DNS results.
